1.20190709Project文件中使用了速度环、电流环和功率环三环路并联控制输出，输出最小值PWM。
2.使用增量式PI控制器
3.方案评审中出现的需求变更，
（1）.多电压设置更改为宽范围的电压10V到50V，以便适用于范围内不同型号的电池。
（2）.将上电自校正更改为用户使用方向盘校正。
（3）.将每次上电自校正更改为用户仅校正一次，对于断电记录当前船机的位置值提出一些方案：
对于方向盘关机时刻发送关机标志位给控制器，控制器flash记录当前的位置，防止断电丢失；
对于控制器那边突然断电，方向盘记录了当前位置，待控制器与方向盘联接上后将控制器断电时刻的值由方向盘发给控制器，以便控制器记录上一断电时刻的位置值。
对于方向盘断电，    则控制器那边检测到方向盘通信异常，保存当前位置，
对于方向盘同时断电，则需要用户重新校正。


9.24新版本添加了，方向盘关机后开机可以自校正功能，用户就不需要断驱动的电即可再次自校正丝杆，这部分存在不足，屏蔽了。
修正了hall没有连接时候的不会报hall故障的功能。

9.26 ，测试自校正hall，加入串口数据打印。

9.27 ，串口数据打印，将hall的概率性报解决。